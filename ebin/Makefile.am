beamdir = $(ERLANG_INSTALL_LIB_DIR_sysloggerl)/ebin

APP_FILES = sysloggerl.app sysloggerl.appup
BEAM_FILES = *.beam

beam_DATA = $(APP_FILES) $(BEAM_FILES)

clean-local:
	$(AM_V_at)rm -f $(APP_FILES) $(BEAM_FILES)

# Local Variables:
#    tab-width: 8
# End:
